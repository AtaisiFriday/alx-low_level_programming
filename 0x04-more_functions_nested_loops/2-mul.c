#include "main.h"
#include <stdio.h>

/**
 * mul - Multiplies two integers
 * @a: -The first integer
 * @b: - The second integer
 * Return: always 0(success)
 */

int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
