#include <stdio.h>
/**
 * main -Entry point
 * Description
 * Return: always (0)
 */
int main(void)
{
	char lc = 'a';

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
	}
		putchar('\n');
	return (0);
}
