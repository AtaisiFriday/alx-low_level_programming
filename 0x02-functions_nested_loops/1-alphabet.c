#include "main.h"
/**
 * main -Entry point
 * Description
 * Return: always (0)
 */
void print_alphabet(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		_putchar(lc);
	}
		_putchar('\n');
	return (0);

}
